//I affirm that all code given below was written solely by me, Sara Wescott, and that any help I received adhered to the rules stated for this exam. 

#include <string>
#include <iostream>
#include "Venue.h"
#include "Event.h"
using namespace std;


void Venue::addEvent(int const time , string name)
{
	Event ScheduledEvents[time] = name;
}

bool Venue::validTime(const int time)
{
	bool empty(Event ScheduledEvents[time]);
}

Venue::findEvent(int time)
{
	Event ScheduledEvents[time];
}

Venue::findEvent(string name)
{
	for (int i = 0; i < 11; i++)
	{
		if (Event ScheduledEvents[i] = name)
		{

		}
	}
}